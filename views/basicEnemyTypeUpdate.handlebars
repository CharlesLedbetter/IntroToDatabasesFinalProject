<fieldset class="formcolor">
  <fieldset class="innerFieldset">
    <h1>Basic Enemy Type Data</h1>
    <p> Below is a chart of all basic info regarding the enemy types in the game.</p>
    <p> Use the actions on the chart to add, update, or delete.</p>
  </fieldset>
  <fieldset class="innerFieldset">
    <input type="submit" name="Done" value="Done" id="done" class="wideButton tallButton" onclick="doneButton();">
  </fieldset>
</fieldset>
<fieldset class="formcolor">
  <fieldset class="innerFieldset">
    <legend>Basic Enemy Type Data</legend>
    <table id="print">
      <tr>
       <th>Enemy Type</th>
       <th>Description</th>
       <th>AttackPoints</th>
       <th>DefensePoints</th>
       <th>Item Dropped Upon Death</th>
       <th>Actions</th>
      </tr>
      <tr>
       <th><input type="text" name="TypeAdd" id="add0" class="grayed"></th>
       <th><input type="text" name="DescAdd" id="add1" class="grayed"></th>
       <th><input type="number" name="AttAdd" id="add2" class="grayed" min = 1></th>
       <th><input type="number" name="DefAdd" id="add3" class="grayed" min = 1></th>
       <th>
         <select name="OptionsItem" id="add4">
           <option label="" value="">
           {{#each options}}
            <option label="{{this.option}}" value="{{this.val}}">
           {{/each}}
         </select>
       </th>
       <th><input type="submit" name="add" value="Add" id="insertButton" class="wideButton" onclick="insertButton(document.getElementById('pageID').value);"></th>
      </tr>
      {{#each print}}
        <tr>
          <td>{{this.Type}}</td>
          <td>{{this.Description}}</td>
          <td>{{this.AttackPoints}}</td>
          <td>{{this.DefensePoints}}</td>
          <td>{{this.ItemDrop}}</td>
          <td>
            <input type="submit" name="update" value="Update" class="wideButton" onclick="updateStartButton(this.parentNode.children[1]);"><input type="hidden" name="id" value="{{this.ID}}" id="{{this.ID}}"><input type="submit" name="delete" value="Delete" class="wideButton" onclick="deleteButton(document.getElementById('pageID').value, this.parentNode.children[1]);">
          </td>
        </tr>
      {{/each}}
    </table>
  </fieldset>
</fieldset>
<input type="hidden" id="pageID" value="3">
